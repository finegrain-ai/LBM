[project]
name = "rord"
version = "0.1.0"
description = "Convert the RORD dataset into WebDataset format"
readme = "README.md"
requires-python = ">=3.12.8"
dependencies = [
    "numpy>=1.26.4",
    "opencv-python>=4.11.0.86",
    "pillow>=11.3.0",
    "tqdm>=4.67.1",
]
